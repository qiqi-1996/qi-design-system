/**
 * Common 模块仅根据设计系统定义处理生成值的部分，而非生成特定框架的配置。
 * The 'Common' module only processes the part of the generated value according to the design system definition, not the configuration of a specific framework.
 */

export * from "./color.ts"
export * from "./font.ts"
